{
  "jsonpaths": [
  
    "$.schema.vendor"
    , "$.schema.name"
    , "$.schema.format"
    , "$.schema.version"

    , "$.hierarchy.rootId"
    , "$.hierarchy.rootTstamp"
    , "$.hierarchy.refRoot"
    , "$.hierarchy.refTree"
    , "$.hierarchy.refParent"

    , "$.data.category"
    , "$.data.action"
    , "$.data.label"
    , "$.data.value"
    , "$.data.wish_list"
    , "$.data.video_name"
    , "$.data.video_product_name"
    , "$.data.sku"
    , "$.data.number_of_recs"
    , "$.data.rec_strategy"
    , "$.data.filter_category"
    , "$.data.filter_value"
    , "$.data.video_state"
    , "$.data.video_timestamp"
    , "$.data.video_length"
    , "$.data.style_number"
    , "$.data.star_rating"
    , "$.data.reviews_size_select"
    , "$.data.reviews_age_select"
    , "$.data.reviews_sort_by"
    , "$.data.results_page"
    , "$.data.brand_name"
    , "$.data.number_of_images"
    , "$.data.number_of_videos"
    , "$.data.note_value"
    , "$.data.note_expire_date"
    , "$.data.applied_notes_total"
    , "$.data.available_notes_total"
    , "$.data.gift_card_total"
    , "$.data.gift_card_value"
    , "$.data.page_id"
    , "$.data.search_term"
    , "$.data.number_of_reviews"
    , "$.data.rms_sku"
    , "$.data.web_style_id"
    , "$.data.outfit_id"
    , "$.data.store_number"
    ]
}