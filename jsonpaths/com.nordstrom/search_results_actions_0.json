{
  "jsonpaths": [
  
    "$.schema.vendor"
    , "$.schema.name"
    , "$.schema.format"
    , "$.schema.version"

    , "$.hierarchy.rootId"
    , "$.hierarchy.rootTstamp"
    , "$.hierarchy.refRoot"
    , "$.hierarchy.refTree"
    , "$.hierarchy.refParent"

    , "$.data.action"
    , "$.data.category"
    , "$.data.value"
    , "$.data.tag_id"
    , "$.data.page_url"
    , "$.data.keyword"
    ]
}