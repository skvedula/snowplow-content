{
  "jsonpaths": [
  
    "$.schema.vendor"
    , "$.schema.name"
    , "$.schema.format"
    , "$.schema.version"

    , "$.hierarchy.rootId"
    , "$.hierarchy.rootTstamp"
    , "$.hierarchy.refRoot"
    , "$.hierarchy.refTree"
    , "$.hierarchy.refParent"

    , "$.data.team_id"
    , "$.data.experiment_name"
    , "$.data.parameter_name"
    , "$.data.parameter_value"
    , "$.data.elwin_id"
    ]
}